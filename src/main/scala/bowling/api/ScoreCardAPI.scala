package bowling.api

import bowling.domain.{Player, LaneId, PlayerName, PlayerId}

class ScoreCardAPI {
  def addPlayer(lane: LaneId, playerName: PlayerName):PlayerId = {
    null
  }

  def getPlayers(lane: LaneId):Set[Player] = {
    null
  }
}
